<?xml version="1.0" encoding="utf-8" standalone="no"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" android:installLocation="auto" package="com.robtopx.geometryjump">
    <supports-screens android:anyDensity="true" android:largeScreens="true" android:normalScreens="true" android:smallScreens="true" android:xlargeScreens="true"/>

    <!--
      v0.5 — Stripped all dangerous permissions. Only INTERNET and
      ACCESS_NETWORK_STATE remain (both are "normal" protection-level and
      do not require user consent on Android 6.0+).

      Removed dangerous permissions:
        - READ_PHONE_STATE          (was used for device-id tracking)
        - WRITE_EXTERNAL_STORAGE    (was used to cache downloaded levels;
          the game now uses app-private storage which needs no permission)
        - ACCESS_WIFI_STATE         (was used by analytics; not needed)
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-feature android:name="android.hardware.wifi" android:required="false"/>
    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>

    <!--
      v0.5 — debuggable=false (was true in v0.4, which is dangerous on
      production builds because it lets any debugger attach to the process).
    -->
    <application android:debuggable="false" android:icon="@drawable/icon" android:label="@string/app_name">
        <activity android:configChanges="orientation" android:label="@string/app_name" android:name=".GeometryJump" android:screenOrientation="landscape" android:theme="@android:style/Theme.NoTitleBar.Fullscreen" android:windowSoftInputMode="stateAlwaysHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
