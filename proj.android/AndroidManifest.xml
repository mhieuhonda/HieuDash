<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.robtopx.geometryjump"
    android:versionCode="21"
    android:versionName="1.9"
    android:installLocation="auto">

    <!-- Minimum API 9 (Gingerbread) — matches the original shipped APK.
         Target API 28 so it runs on modern Android without compatibility
         warnings. -->
    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="28" />

    <!-- Permissions carried over from the original APK -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- OpenGL ES 1.0 required by Cocos2d-x 2.x -->
    <uses-feature android:glEsVersion="0x00010000" android:required="true" />

    <application
        android:label="@string/app_name"
        android:icon="@drawable/icon"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:debuggable="false">

        <!-- Main game activity — provided by Cocos2d-x Java layer -->
        <activity
            android:name="org.cocos2dx.lib.Cocos2dxActivity"
            android:label="@string/app_name"
            android:screenOrientation="landscape"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|screenLayout|uiMode"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>
</manifest>
