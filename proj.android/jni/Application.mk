# Application.mk for Hieu Dash (Cocos2d-x 2.2.3)
# Reconstructed from the original Geometry Dash 1.0 build configuration.

APP_STL := gnustl_static
APP_CPPFLAGS := -fexceptions -frtti -DCOCOS2D_DEBUG=1
APP_ABI := armeabi
APP_PLATFORM := android-8

# Match the original libgame.so: armeabi only, gnustl_static STL.
