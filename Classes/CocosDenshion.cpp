#include "CocosDenshion.h"

// CocosDenshion is a NAMESPACE in cocos2d-x 2.2.3, not a class.
// The real SimpleAudioEngine implementation lives in
// cocos2d/CocosDenshion/linux/SimpleAudioEngineOpenAL.cpp (Linux) or
// cocos2d/CocosDenshion/win32/*.cpp (Windows). The Hieu Dash project
// compiles those directly via the CMakeLists.txt COCOS_DENSHION_SOURCES
// variable, so this file is intentionally empty - it exists only so the
// build system has a .cpp to compile for the CocosDenshion.h header.
